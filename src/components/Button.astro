---
interface Props {
    href: string;
    variant?: "primary" | "secondary";
    class?: string;
    fullWidth?: boolean;
}

const {
    href,
    variant = "primary",
    class: className = "",
    fullWidth = false,
} = Astro.props;
---

<a
    href={href}
    class:list={[
        "inline-flex items-center justify-center px-6 py-4 text-center font-medium rounded-xl transition-all duration-200",
        fullWidth && "w-full",
        variant === "primary" &&
            "bg-gradient-to-r from-orange-500 to-red-600 text-white shadow-lg shadow-orange-500/30 hover:shadow-xl hover:shadow-orange-500/40 hover:-translate-y-0.5",
        variant === "secondary" &&
            "bg-white/90 text-slate-900 border border-slate-200 hover:bg-white hover:border-slate-300",
        className,
    ]}
>
    <slot />
</a>
